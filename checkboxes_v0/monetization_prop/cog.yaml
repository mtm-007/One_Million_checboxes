# Configuration for Cog ⚙️
# Reference: https://github.com/replicate/cog/blob/main/docs/yaml.md

build:
  gpu: True
  cuda: "11.8"
  python_version: "3.11"
  system_packages:
    - "wget"
    - "git"
    - "libgl1"
    - "libglib2.0-0"
    - "ffmpeg"

  python_packages:
    - "diffusers==0.28.2"
    - "torch==2.0.1"
    - "transformers==4.36.0"
    - "huggingface-hub==0.20.2"
    - "invisible-watermark==0.2.0"
    - "accelerate==0.21.0"
    - "pandas==2.0.3"
    - "torchvision==0.15.2"
    - "numpy==1.26.4"
    - "pandas==2.0.3"
    - "fire==0.5.0"
    - "opencv-python==4.11.0.86"
    - "mediapipe==0.10.21" 

    #- "tokenizers==0.12.0"  #let transformers handle it

  # run:
  #   - "mkdir -p /src/my_diffusion_pipeline"

  #commands run after the environment is setup
  # run:
  #   - "echo env is ready!"
  #   - "echo another command if needed"
  
predict: "predict.py:Predictor"